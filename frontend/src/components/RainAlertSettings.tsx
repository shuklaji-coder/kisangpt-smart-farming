import React from 'react';
import { Box, Typography } from '@mui/material';

const RainAlertSettings = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Rain Alert Settings
      </Typography>
      <Typography>
        Feature coming soon! This will allow farmers to configure rain alerts.
      </Typography>
    </Box>
  );
};

export default RainAlertSettings;